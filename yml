spring:
  datasource:
    hikari:
      maximum-pool-size: 8        # максимум на под
      minimum-idle: 2             # минимум idle
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
